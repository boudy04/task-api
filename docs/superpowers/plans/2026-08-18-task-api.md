# Task API Implementation Plan

> Spec: `docs/superpowers/specs/2026-08-18-task-api-design.md`
> Project: `p3/` — FastAPI task-tracker CRUD API with Postgres, token auth,
> Docker, CI, Railway deploy. Fed by a Kotlin app in Week 6–7.

## Overview

Build and ship a public CRUD REST API (FastAPI + Postgres + SQLAlchemy).
Each task ends with a runnable test cycle (pytest) against a real Postgres.
Order: environment first (Docker is a hard prerequisite for real-PG tests),
then scaffold, then data layer, then endpoints, then packaging/deploy.

## Global constraints

- Python 3.11 via **uv** (available on this machine). No pip in uv venvs — use
  `uv add` / `uv run`.
- **Docker Desktop is NOT installed** and WSL2/Hyper-V are absent — Task 1
  installs them. Requires admin + a reboot; the plan pauses at a resume gate.
- Local account is Windows admin → embedded Postgres binaries are blocked by
  Postgres itself ("administrative permissions not permitted"). Real Postgres
  must come from a container.
- Postgres binary zip was already downloaded to
  `C:\Users\Admin\AppData\Local\Temp\opencode\pgbin\pgsql\bin` during the
  spike — NOT part of the project; used only for ad-hoc `initdb` checks. Do not
  reference it in project code.
- Git identity: `boudy04 <boudy04@users.noreply.github.com>`.
- GitHub push pattern (from prior projects): embed PAT in remote URL, push,
  then `git remote set-url origin https://github.com/boudy04/<repo>.git`.
  git-stats PAT works only for git-stats; task-api may need its own or the
  classic PAT from `~/.git-credentials`.

## Dependencies

- `fastapi`, `uvicorn[standard]`, `sqlalchemy`, `psycopg[binary]`,
  `pydantic-settings` (runtime); `pytest`, `httpx` (TestClient transport) (test).
- Docker Desktop (Task 1) provides `docker compose`.

## Current structure

```
p3/
  .git/                      # initialized, spec committed
  docs/superpowers/specs/2026-08-18-task-api-design.md
```

## Tasks

### 1. Install WSL2 + Docker Desktop (env gate)

**Summary:** Install the Docker prerequisite: WSL2 kernel + Docker Desktop,
start the engine, verify `docker compose` works.

**Files:** none (system install)

**Test cycle:**
1. `wsl --install` (admin shell) → reboot required
2. Resume here after reboot; `wsl --status` shows a running distro
3. Install Docker Desktop (winget: `winget install -e --id Docker.DockerDesktop`)
4. `docker version` and `docker compose version` succeed
5. `docker run --rm hello-world` exits 0

**Duration:** ~45 min install + reboot

**Gate:** `docker run --rm hello-world` succeeds. **If WSL install fails on
this machine, stop and return to the user** — a local Postgres via container is
a hard dependency of every later task.

---

### 2. Scaffold project + config

**Summary:** Create `pyproject.toml` (uv), app package skeleton, and
`app/config.py` (pydantic-settings) reading `DATABASE_URL` + `AUTH_TOKEN` with
sane defaults.

**Files:** `pyproject.toml`, `.gitignore`, `app/__init__.py`, `app/config.py`

**Test cycle:** `tests/test_config.py` — assert defaults when env vars unset
(`AUTH_TOKEN` non-empty default, `DATABASE_URL` points at
`postgresql+psycopg://...localhost:5432/taskapi`), and env override works.

**Duration:** 15 min

**Gate:** `uv run pytest` green.

---

### 3. docker-compose Postgres + test fixtures

**Summary:** Add `docker-compose.yml` (postgres:16 only, port 5432, user
`postgres`, db `taskapi`), `tests/conftest.py` (engine from config, create
schema once per session, truncate `tasks` between tests), and a
`tests/test_db.py` smoke test proving a real query round-trips.

**Files:** `docker-compose.yml`, `tests/conftest.py`, `tests/test_db.py`

**Test cycle:**
1. `docker compose up -d postgres` (from Task 1's working Docker)
2. `tests/test_db.py` — `SELECT 1` via SQLAlchemy engine succeeds, table
   create/drop round-trip works
3. `uv run pytest` green

**Duration:** 25 min

**Gate:** `tests/test_db.py` passes against the containerized Postgres.

---

### 4. Models + schemas (data contracts)

**Summary:** Add `app/models.py` (SQLAlchemy `Task`: id, title, description,
status, priority, created_at, updated_at, per spec table) and `app/schemas.py`
(pydantic `TaskCreate` — title required, status/priority validated enums with
defaults; `TaskUpdate` — all optional; `TaskRead` with id + timestamps).

**Files:** `app/models.py`, `app/schemas.py`, `tests/test_schemas.py`

**Test cycle:** `tests/test_schemas.py` — valid TaskCreate accepted; missing
title → 422; invalid status/priority → 422; defaults applied; TaskUpdate
accepts empty subset.

**Duration:** 20 min

**Gate:** `uv run pytest` green.

---

### 5. db session + auth dependency

**Summary:** Add `app/db.py` (engine, `SessionLocal`, `Base`,
`get_db` dependency) and `app/auth.py` (`get_current_token` dependency: reads
`Authorization: Bearer <token>`, compares to `AUTH_TOKEN`, 401 JSON on
missing/mismatch).

**Files:** `app/db.py`, `app/auth.py`, `tests/test_auth.py`

**Test cycle:** `tests/test_auth.py` — need a minimal FastAPI app wired with the
dependency. 401 for missing header, 401 for wrong token, 200 for valid token.
(Test app lives in the test file; real wiring comes in Task 7.)

**Duration:** 20 min

**Gate:** `uv run pytest` green.

---

### 6. Tasks router (all CRUD)

**Summary:** Add `app/routers/tasks.py` implementing all six endpoints per
spec: GET list (with `?status=` filter, newest first), POST create (201),
GET by id, PUT full update, PATCH partial, DELETE (204); 404 JSON
`{"detail": "Task not found"}` for unknown ids.

**Files:** `app/routers/tasks.py`, `tests/test_tasks.py`

**Test cycle:** `tests/test_tasks.py` against real PG —
1. POST → 201, body echoes id + defaults
2. GET list → newest first; `?status=done` filters
3. GET unknown id → 404 JSON
4. PUT replaces fields; PUT unknown id → 404
5. PATCH `{"status": "done"}` changes only status
6. DELETE → 204; second DELETE → 404
7. Invalid body → 422

**Duration:** 40 min

**Gate:** `uv run pytest` green (full suite).

---

### 7. main.py wiring + /health

**Summary:** Add `app/main.py` (FastAPI app, include tasks router under `/api`,
public `/health` → `{"status": "ok"}`, wire `get_db` + auth dependencies) and
`tests/test_health.py`.

**Files:** `app/main.py`, `tests/test_health.py`

**Test cycle:** `tests/test_health.py` — `/health` 200 with no auth; app boots
via TestClient; `/api/tasks` without token → 401.

**Duration:** 15 min

**Gate:** `uv run pytest` green. Commit point: the whole API works locally.

---

### 8. Dockerfile + full compose + README

**Summary:** Add `Dockerfile` (python:3.12-slim, pip-install `.[prod]`, uvicorn
CMD), extend `docker-compose.yml` (api service depends on postgres, builds from
Dockerfile, `AUTH_TOKEN` + `DATABASE_URL` env), and `README.md` (what it is,
stack, run with compose, curl examples, test command). Add a `[project.optional-dependencies]` prod extra if the Dockerfile needs it.

**Files:** `Dockerfile`, `docker-compose.yml` (edit), `README.md`,
`pyproject.toml` (edit)

**Test cycle:**
1. `docker build -t task-api .` exits 0
2. `docker compose up --build -d` brings up api+postgres
3. `curl http://localhost:8000/health` → `{"status":"ok"}`
4. `curl` a POST with a bearer token → 201

**Duration:** 30 min

**Gate:** containerized API answers `/health` and a token-authed POST.

---

### 9. CI workflow

**Summary:** Add `.github/workflows/ci.yml` — on push: uv setup, install, run
`pytest` with a Postgres 16 service container (`DATABASE_URL` + `AUTH_TOKEN`
env set to match compose values).

**Files:** `.github/workflows/ci.yml`

**Test cycle:** push the repo to GitHub (Task 10), watch the action go green.

**Duration:** 20 min

**Gate:** CI workflow green on the pushed commit. (Needs Task 10 to run — do
10a push first if gating.)

---

### 10. GitHub repo + pin

**Summary:** Create public repo `task-api` on GitHub, push (embedded-PAT pattern),
pin to profile. Set CI to run on push.

**Files:** none (git remote + GitHub UI)

**Test cycle:** `git push -u origin main` succeeds; repo appears in profile.

**Duration:** 20 min

**Gate:** pushed; CI (Task 9) green on GitHub.

---

### 11. Railway deploy + live verification

**Summary:** Create Railway project from the GitHub repo (Dockerfile), attach
Postgres plugin, set `DATABASE_URL` (Railway-provided, ensure `+psycopg`
dialect) + `AUTH_TOKEN`. Verify `/health` reachable publicly and one authed
CRUD round-trip via curl.

**Files:** none (Railway dashboard)

**Test cycle:**
1. `curl https://<app>.up.railway.app/health` → `{"status":"ok"}`
2. Authed POST → 201, GET list → contains it

**Duration:** 30 min

**Gate:** live `/health` + CRUD on the public URL. **Final DoD check against
the spec checklist.**

---

## Concurrency

Serial. Every task depends on the previous one (Docker → compose DB → data
layer → endpoints → packaging → deploy). No parallelizable independent tracks
except the README, which could be drafted in parallel with Task 6–7.

## Final verification

- Full `uv run pytest` green locally against containerized Postgres.
- `docker build` + `docker compose up --build` healthy; `/health` answers.
- CI green on GitHub for the pushed `main`.
- Public Railway URL answers `/health`; authed CRUD works end-to-end.
- Spec DoD checklist all boxes ticked; update `CV-PLAN.md` Week 3–5 row.